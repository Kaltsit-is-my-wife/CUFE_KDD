# CUFE_KDD 项目记忆

## 项目定位

- 这是数据挖掘团队的多人协作知识库和实验原型仓库，不是生产代码仓库。
- 仓库记录团队思考、头脑风暴、文献笔记、实验方案、讨论记录和原型代码。
- 团队规模按至少 5 名成员协作设计，成员占位符使用 `@member-01` 至 `@member-05`。

## 结构记忆

- `sandbox/`：个人临时草稿和快速验证，不保证复现；成熟内容迁移出去。
- `ideas/`：问题、背景、文献、讨论、核心假设和实验设计。
- `experiments/`：与 idea 使用相同三位编号的可运行实验原型、指标摘要和结果图片。
- `PROGRESS.md`：所有 idea 和实验的统一进度表。
- `000-template`：模板目录，不是实际 idea 或实验。

## 不可违反的规则

1. 先写 idea，再落地实验；idea 与实验必须编号一一对应。
2. 正式目录使用小写、短横线和三位补零编号，例如 `001-short-name-idea`。
3. 废弃内容标记为 `archived` 并保留历史，不删除。
4. 原始数据集、模型权重、大日志、凭据和受限数据禁止提交 Git。
5. 只提交预处理脚本、指标摘要、Markdown 文档、脱敏小样例和可审阅图片。
6. 变更必须通过 PR，至少一名成员 review，禁止直接 push `main`。
7. 每个 idea 和实验都必须明确记录【核心假设】、基线、数据集、指标和限制。

## 工作偏好

- 保持目录层级浅，优先在现有模板上增量修改。
- 代码是研究/原型代码，重点是可理解、可比较、可记录，不假设生产级稳定性。
- 任何实验结论都要区分观察结果、解释、未验证因素和下一步。
- 运行命令、环境、随机种子、数据版本和外部大文件位置应记录在实验文档中。
